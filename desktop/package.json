{
  "name": "ahram-desktop",
  "private": true,
  "version": "1.2.1",
  "main": "build/main/main.js",
  "scripts": {
    "dev": "node ../node_modules/typescript/bin/tsc -p tsconfig.json && electron .",
    "build:ts": "node ../node_modules/typescript/bin/tsc -p tsconfig.json",
    "build": "node ../node_modules/typescript/bin/tsc -p tsconfig.json && electron-builder",
    "tsc": "node ../node_modules/typescript/bin/tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "electron": "^33.4.0",
    "electron-builder": "^25.0.0"
  }
}
